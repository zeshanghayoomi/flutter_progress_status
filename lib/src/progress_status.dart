import 'package:flutter/material.dart';

class ProgressStatus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Progress Status Widget'),
    );
  }
}
