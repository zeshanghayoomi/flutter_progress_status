name: flutter_progress_status
description: A simple widget that can be used to display progress status of a task, use the provided properties to customise the way you like.
version: 0.0.3
homepage: https://github.com/zeshanghayoomi/flutter_progress_status

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

